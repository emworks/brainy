<style>
    html,
    body {
        margin: 0;
        padding: 5px;
    }

    h1 {
        font-size: 16px;
    }

    table {
        border-collapse: collapse;
        font-size: 12px;
    }

    th,
    td {
        border: 1px solid;
        padding: 5px;
    }
</style>

<div>
    <h1>
        Результаты поиска<%= q ? ' "' + q + '"' : '' %>
    </h1>
    <table>
        <thead>
            <th>Название курса</th>
            <th>Источник</th>
            <th>Язык</th>
            <th>Рейтинг</th>
            <th>Дата</th>
        </thead>
        <tbody>
            <% for (var i = 0; i < data.length; i++) { %>
            <tr>
                <td>
                    <a href="<%= data[i].url %>" target="_blank">
                        <%= data[i].title %>
                    </a>
                </td>
                <td>
                    <%= data[i].sourceId || '-' %>
                </td>
                <td>
                    <%= 
                        data[i].lang ? ([
                            'English', 'Русский'
                        ])[data[i].lang - 1] :  '-' 
                    %>
                </td>
                <td>
                    <%= data[i].rating ? data[i].rating + '/5' : '-' %>
                </td>
                <td>
                    <%= data[i].dateFrom || '-' %>
                </td>
            </tr>
            <% } %>
        </tbody>
    </table>
</div>